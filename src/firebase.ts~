import { initializeApp } from "firebase/app"
import { getAuth, GoogleAuthProvider } from "firebase/auth"
import { getFirestore } from "firebase/firestore/lite"

export const firebaseConfig = {
  apiKey: "AIzaSyBvV-Cz5Cie8k7deLkwhOy_wn3nNtsmpHQ",
  authDomain: "vue-chat-58201.firebaseapp.com",
  projectId: "vue-chat-58201",
  storageBucket: "vue-chat-58201.firebasestorage.app",
  messagingSenderId: "676873332523",
  appId: "1:676873332523:web:2f917a456261609f58476e"
};

export const firebaseApp = initializeApp(firebaseConfig);

const auth = getAuth(firebaseApp);

const db = getFirestore(firebaseApp);

export const provider = new GoogleAuthProvider();
